# Support

Start with [Lookup](docs/lookup.md) and [Definitions](docs/definitions.md).
For help, open an [issue](https://github.com/altophp/language/issues) with PHP and
package versions, the exact input identifier, lookup method, expected metadata,
and any application registrations. Include the result of `conflicts()` when
multiple definitions use the same alias, extension, or filename.
